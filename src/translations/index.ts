import { en } from "./en";
import { es } from "./es";

export type Language = "en" | "es";

export const translations = {
  en,
  es,
};

export const languageNames = {
  en: "English",
  es: "Español",
};
